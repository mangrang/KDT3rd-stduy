<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/result26" name="loginForm" method="get">
        <label for="nameInput">이름</label>
        <input type="text" name="nameInput">
        <br>
        
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="genderForm" value="male">남자
            <input type="radio" name="genderForm" value="female">여자
        </fieldset>
        <br>
        <fieldset>
            <legend>생년월일</legend>
            <select name="year" id="year">
                <% for(var i = 1; i < 50; i++){ %>
                    <option value=<%= 2010 - i %>><%= 2010 - i %></option>
                   <% } %>
            </select>년
            <select name="month" id="month">
                <% for(var i = 1; i < 13; i++){ %>
                    <option value=<%= i %>><%= i %></option>
                   <% } %>
            </select>월
            <select name="day" id="day">
                <% for(var i = 1; i < 32; i++){ %>
                    <option value=<%= i %>><%= i %></option>
                   <% } %>
            </select>일

        </fieldset>
        <br>
        <fieldset>
            <legend>관심사</legend>
            <label><input type="checkbox" name="hobbyForm" value="여행">여행</label>
            <label><input type="checkbox" name="hobbyForm" value="패션">패션</label>
            <label><input type="checkbox" name="hobbyForm" value="음식">음식</label>
        </fieldset>
        <br>
        <button type="submit">회원가입</button>
    </form>
</body>
</html>